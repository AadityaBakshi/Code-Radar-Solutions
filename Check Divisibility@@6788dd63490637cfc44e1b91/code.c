#include<stdio.h>
int main(){
    int A single number ;
    scanf("%d",& A single number);
    if(A single number%5==0 && A single number%11==0){
        printf("Divisble");
    }
    else{
        printf("Not Divisble");
}
    
    return 0;
}